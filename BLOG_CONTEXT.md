# BLOG_CONTEXT.md — 蔡蔡的技术博客统一规范

> 把这份文件放在博客项目根目录。每次让任何 AI 帮你写博客文章时，告诉它："请参考项目根目录的 BLOG_CONTEXT.md 来写这篇文章。"

---

## 博客基本信息

| 项目 | 内容 |
|------|------|
| 博客名称 | 蔡蔡的技术笔记 |
| 博客地址 | https://caixinying-less-is-more.github.io/blog/ |
| 框架 | Hugo |
| 文章路径 | `content/posts/` |
| 主题 | PaperMod |
| 语言 | 中文为主，必要时中英混排 |
| 定位 | 0 基础转行机器人算法的技术学习博客 |
| 作者 | 蔡昕颖（蔡蔡）🐝 |
| 背景 | 2020-2024 本科浙大能源与环境系统工程 / 2025-至今 博士港城大材料科学与工程 |
| 自定义样式 | `assets/css/extended/custom.css`（蜂蜜琥珀色调） |

## 文章 Frontmatter 模板

每篇文章开头必须包含以下 YAML frontmatter。这是 Hugo 解析文章元数据的关键，格式出错会导致文章不显示。

```yaml
---
title: "文章标题——一句话概括核心内容"
date: 2026-08-03T22:00:00+08:00
draft: false
tags: ["标签1", "标签2", "标签3"]
categories: ["分类名"]
description: "这篇文章的核心观点，1-2句话，用于 SEO 和社交分享预览"
---
```

### 分类体系（categories）

当前预定义的分类（只能从下面选，不要自创）：

- `机器人` — 控制算法、动作空间、策略表示、机器人平台、数据采集
- `AI与工具` — 扩散模型、机器学习方法、AI 使用技巧、自动化工具
- `职业规划` — PhD 就业、方向选择、技能路线
- `学术研究` — 文献笔记、论文路线、研究方向分析

### 内容组织原则

- 博客按**主题**组织，不按日期打包。每个主题一篇持续扩展的文章。
- 新知识点追加到对应主题文章的末尾，不是每天发一篇新文章。
- 文章末尾标注 `*最后更新：YYYY-MM-DD*`。
- 不记录任何材料科学/钙钛矿/化学相关内容。

### 标签体系（tags）

标签可以自由添加，建议优先从以下常用标签中选择：

材料相关：`钙钛矿` `半导体` `柔性电子` `电子皮肤` `触觉传感器` `压电材料` `水凝胶` `薄膜`
机器人相关：`ROS2` `MuJoCo` `人形机器人` `机械臂` `传感器集成` `LQR` `MPC` `控制理论` `动作空间` `策略表示` `6DoF`
AI相关：`DDPM` `DDIM` `Diffusion Policy` `扩散模型` `Hugo` `GitHub` `Cloudflare` `自动化` `Python` `Markdown`
论文相关：`论文阅读` `具身智能` `VLA` `学习路线` `遥操作` `数据采集` `Co-training`
职业相关：`PhD` `就业` `实习` `面试`

---

## 写作风格指南

### 语气

- 技术博客，但不要太学术论文——像在跟同行交流，可以放松但不失专业
- 可以有个性、有观点，但观点要有数据或逻辑支撑
- 不要过度包装，说清楚就好

### 结构

- 开头 2-3 句话说明这篇文章解决什么问题、适合谁读
- 正文分成章节，用 `##` 做二级标题
- 结尾总结核心观点 + （可选）下一步行动建议
- 如果有参考文献或链接，放在文末 `## 参考` 小节

### 格式

- 代码块用三个反引号标注语言，不加额外修饰
- 数据对比优先用表格，不要用文字描述
- 列表项用 `-`，不要用 `*`
- 英文专有名词前后加空格，如 "使用 ROS2 控制 UR5e 机械臂"

---

## 发布流程

### 用 WorkBuddy / Claude Code 发布（推荐——全自动）

直接在对话中说："帮我把刚才的讨论整理成文章，按 BLOG_CONTEXT.md 规范发布到博客。"

AI 会自动：
1. 生成带 frontmatter 的 Markdown → 保存到 `content/posts/`
2. `git add && git commit && git push`
3. GitHub Pages 自动部署，1-3 分钟后在 `caixinying-less-is-more.github.io/blog/` 可见

### 用 ChatGPT 发布（需要手动一步）

1. 把 BLOG_CONTEXT.md 附在对话中，让 ChatGPT 生成文章
2. 复制生成的 Markdown，保存到本地 `content/posts/xxx.md`
3. 手动 `git push`
4. 部署自动完成

---

## 图片/图表处理

- 图片放在 `static/images/YYYY/` 目录下（按年份分文件夹）
- 引用方式：`![描述](/images/YYYY/filename.png)`
- 优先用 SVG 或 PNG；截图用 PNG
- 图片文件名：`YYYY-MM-DD-topic.png` 格式，如 `2026-08-03-ros2-architecture.png`

---

## 注意事项

1. 文件名用英文加连字符，如 `robot-tactile-sensor-materials.md`
2. `draft: false` 才会公开发布；写作中设为 `true` 可在本地预览
3. 不要写太长——普通人阅读舒适区在 1500-3000 字
4. 给文章加一个 `description`，这对搜索引擎可见的摘要很重要
